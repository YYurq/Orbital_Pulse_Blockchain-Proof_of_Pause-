name: Anchor Build

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3

      - name: Build Everything
        run: |
          set -e
          
          echo "=== Installing System Dependencies ==="
          sudo apt-get update
          sudo apt-get install -y pkg-config build-essential libudev-dev libssl-dev
          
          echo "=== Installing Latest Rust Nightly ==="
          curl --proto '=https' --tlsv1.2 -sSf https://sh.rustup.rs | sh -s -- -y --default-toolchain nightly
          source $HOME/.cargo/env
          rustup component add rustfmt clippy
          
          echo "=== Rust Version ==="
          rustc --version
          cargo --version
          
          echo "=== Installing Solana ==="
          wget -q https://github.com/solana-labs/solana/releases/download/v1.17.0/solana-release-x86_64-unknown-linux-gnu.tar.bz2
          tar jxf solana-release-x86_64-unknown-linux-gnu.tar.bz2
          export PATH="$GITHUB_WORKSPACE/solana-release/bin:$PATH"
          
          echo "=== Installing Anchor CLI ==="
          npm install -g @coral-xyz/anchor-cli@0.29.0
          
          echo "=== CRITICAL: Force regenerate Cargo.lock with NEW Cargo ==="
          # Удаляем все Lock везде
          rm -f Cargo.lock
          rm -f programs/*/Cargo.lock
          find . -name "Cargo.lock" -type f -delete
          
          # Генерируем новый Lock v4 с Cargo 1.95
          cargo generate-lockfile
          
          # Проверяем версию Lock
          echo "Generated Cargo.lock version:"
          head -1 Cargo.lock
          
          echo "=== Setting up Solana keypair ==="
          mkdir -p ~/.config/solana
          solana-keygen new --no-bip39-passphrase -o ~/.config/solana/id.json || true
          
          echo "=== Starting Anchor Build ==="
          export RUSTC_BOOTSTRAP=1
          anchor build
          
          echo "=== Build Complete ==="
