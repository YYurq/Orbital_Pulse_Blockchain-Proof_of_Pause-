name: Anchor Build

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Code
        uses: actions/checkout@v3

      - name: Create Local Fix
        run: |
          # Фикс для constant_time_eq (решает проблему Edition 2024)
          mkdir -p fix/constant_time_eq/src
          cat > fix/constant_time_eq/Cargo.toml <<EOF
          [package]
          name = "constant_time_eq"
          version = "0.4.2"
          edition = "2021"
          EOF
          cat > fix/constant_time_eq/src/lib.rs <<EOF
          pub fn constant_time_eq(a: &[u8], b: &[u8]) -> bool {
              if a.len() != b.len() { return false; }
              let mut res = 0;
              for i in 0..a.len() { res |= a[i] ^ b[i]; }
              res == 0
          }
          EOF

      - name: Install System Deps
        run: |
          sudo apt-get update
          sudo apt-get install -y pkg-config build-essential libudev-dev libssl-dev

      - name: Install Solana
        run: |
          wget -q https://github.com/solana-labs/solana/releases/download/v1.18.12/solana-release-x86_64-unknown-linux-gnu.tar.bz2
          tar jxf solana-release-x86_64-unknown-linux-gnu.tar.bz2
          echo "$GITHUB_WORKSPACE/solana-release/bin" >> $GITHUB_PATH

      - name: Install Anchor
        run: npm install -g @coral-xyz/anchor-cli@0.29.0

      - name: Build
        run: |
          export PATH="$GITHUB_WORKSPACE/solana-release/bin:$PATH"
          
          # Настройка ключа
          mkdir -p ~/.config/solana
          solana-keygen new --no-bip39-passphrase -o ~/.config/solana/id.json || echo "exists"
          
          # 1. Генерируем чистый лок-файл
          rm -f Cargo.lock
          cargo generate-lockfile
          
          echo "=== КАЛЛИБРОВКА ЗАВИСИМОСТЕЙ ==="
          
          # Активируем локальный патч
          cargo update -p constant_time_eq
          
          # Откатываем Borsh
          cargo update -p borsh@1.6.0 --precise 1.5.1
          
          # КРИТИЧЕСКИЙ ШАГ: Откатываем proc-macro-crate, чтобы он не требовал новый toml_edit
          cargo update -p proc-macro-crate --precise 3.1.0
          
          # Теперь эти откаты сработают без конфликтов
          cargo update -p toml_edit --precise 0.22.15
          cargo update -p toml_datetime --precise 0.6.3
          
          # Исправление формата лок-файла для Solana (v4 -> v3)
          sed -i 's/version = 4/version = 3/g' Cargo.lock
          
          # Сборка программы
          anchor build
