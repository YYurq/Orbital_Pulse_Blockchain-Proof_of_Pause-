name: Anchor Build

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3

      - name: Build Program
        run: |
          set -e
          
          echo "=== 1. System Dependencies ==="
          sudo apt-get update
          sudo apt-get install -y pkg-config build-essential libudev-dev libssl-dev
          
          echo "=== 2. Install Rust Nightly (Fresh) ==="
          curl --proto '=https' --tlsv1.2 -sSf https://sh.rustup.rs | sh -s -- -y --default-toolchain nightly
          source $HOME/.cargo/env
          rustup default nightly
          
          echo "=== 3. Install Solana Toolchain ==="
          wget -q https://github.com/solana-labs/solana/releases/download/v1.17.0/solana-release-x86_64-unknown-linux-gnu.tar.bz2
          tar jxf solana-release-x86_64-unknown-linux-gnu.tar.bz2
          # Добавляем в PATH текущей сессии
          export PATH="$HOME/.cargo/bin:$GITHUB_WORKSPACE/solana-release/bin:$PATH"
          # Добавляем в PATH для будущих шагов GitHub Actions
          echo "$HOME/.cargo/bin" >> $GITHUB_PATH
          echo "$GITHUB_WORKSPACE/solana-release/bin" >> $GITHUB_PATH
          
          echo "=== 4. Install Anchor CLI ==="
          npm install -g @coral-xyz/anchor-cli@0.29.0
          
          echo "=== 5. Clean and Regenerate Lockfile ==="
          # КРИТИЧНО: Удаляем старый лок, чтобы не было ошибки парсинга
          rm -f Cargo.lock
          find . -name "Cargo.lock" -type f -delete
          
          # Теперь Cargo 1.95 создаст свой Lock
          cargo generate-lockfile
          
          echo "=== 6. Setup Solana Keypair ==="
          mkdir -p ~/.config/solana
          solana-keygen new --no-bip39-passphrase -o ~/.config/solana/id.json || echo "Key exists"
          
          echo "=== 7. Build Anchor Program ==="
          # Подгружаем переменные окружения
          export CARGO=$(which cargo)
          export RUSTC=$(which rustc)
          export RUSTC_BOOTSTRAP=1
          
          echo "Using Cargo path: $CARGO"
          $CARGO --version
          
          anchor build
          
          echo "=== BUILD COMPLETE ==="
