name: Anchor Build

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Code
        uses: actions/checkout@v3

      - name: Hard Cleanup
        run: |
          find . -name "Cargo.lock" -type f -delete
          rm -f Cargo.lock

      - name: Build Program
        run: |
          set -e
          
          echo "=== 1. System Dependencies ==="
          sudo apt-get update
          sudo apt-get install -y pkg-config build-essential libudev-dev libssl-dev
          
          echo "=== 2. Install Rust Nightly ==="
          curl --proto '=https' --tlsv1.2 -sSf https://sh.rustup.rs | sh -s -- -y --default-toolchain nightly
          source $HOME/.cargo/env
          echo "$HOME/.cargo/bin" >> $GITHUB_PATH
          rustup default nightly
          
          echo "=== 3. Install Solana ==="
          wget -q https://github.com/solana-labs/solana/releases/download/v1.17.0/solana-release-x86_64-unknown-linux-gnu.tar.bz2
          tar jxf solana-release-x86_64-unknown-linux-gnu.tar.bz2
          echo "$GITHUB_WORKSPACE/solana-release/bin" >> $GITHUB_PATH
          export PATH="$GITHUB_WORKSPACE/solana-release/bin:$HOME/.cargo/bin:$PATH"
          
          echo "=== 4. Install Anchor ==="
          npm install -g @coral-xyz/anchor-cli@0.29.0
          
          echo "=== 5. Setup Keypair ==="
          mkdir -p ~/.config/solana
          solana-keygen new --no-bip39-passphrase -o ~/.config/solana/id.json || echo "Key exists"
          
          echo "=== 6. Force Downgrade Dependencies ==="
          # Создаем чистый Lock-файл
          cargo generate-lockfile
          
          # Трюк с версией формата (v4 -> v3)
          sed -i 's/version = 4/version = 3/g' Cargo.lock
          
          # ПРИНУДИТЕЛЬНО устанавливаем старые версии из реестра Crates.io
          # Это команды, которые НЕ требуют Git-ссылок и всегда находят пакеты
          echo "Downgrading borsh, blake3 and others..."
          cargo update -p borsh --precise 1.5.1 || echo "borsh update failed"
          cargo update -p blake3 --precise 1.5.1 || echo "blake3 update failed"
          cargo update -p toml_datetime --precise 0.6.3 || echo "toml update failed"
          cargo update -p solana-program --precise 1.17.0 || echo "solana update failed"
          
          echo "=== 7. Build Anchor Program ==="
          export RUSTC_BOOTSTRAP=1
          export PATH="$GITHUB_WORKSPACE/solana-release/bin:$HOME/.cargo/bin:$PATH"
          
          # Итоговый билд
          anchor build
