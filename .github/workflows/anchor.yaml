name: Anchor Build

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Code
        uses: actions/checkout@v3

      - name: Install System Deps
        run: |
          sudo apt-get update
          sudo apt-get install -y pkg-config build-essential libudev-dev libssl-dev

      - name: Install Solana
        run: |
          wget -q https://github.com/solana-labs/solana/releases/download/v1.18.12/solana-release-x86_64-unknown-linux-gnu.tar.bz2
          tar jxf solana-release-x86_64-unknown-linux-gnu.tar.bz2
          echo "$GITHUB_WORKSPACE/solana-release/bin" >> $GITHUB_PATH

      - name: Install Anchor
        run: npm install -g @coral-xyz/anchor-cli@0.29.0

      - name: Build
        run: |
          export PATH="$GITHUB_WORKSPACE/solana-release/bin:$PATH"
          
          # Ключ
          mkdir -p ~/.config/solana
          solana-keygen new --no-bip39-passphrase -o ~/.config/solana/id.json || echo "exists"
          
          # Генерируем лок
          cargo generate-lockfile
          
          # ПРИНУДИТЕЛЬНО откатываем проблемный пакет через реестр
          # Это предотвратит попытку скачать версию 0.4.2 с Rust 2024
          cargo update -p constant_time_eq --precise 0.3.0
          
          # Хак формата для Solana 1.18 (v4 -> v3)
          sed -i 's/version = 4/version = 3/g' Cargo.lock
          
          # Сборка в офлайн режиме, чтобы Cargo не вздумал ничего обновлять обратно
          anchor build -- --offline
